# Standalone docker run only (not the Compose dev service).
# Compose course path: MODEL_BASE_URL_CLEAN, MODEL_BASE_URL_CHAOS, MODEL_API_KEY_DEV
# — see container/compose/.env.example and dev-image.md LiteLLM contract.
LITELLM_BASE_URL=http://localhost:4000
LITELLM_API_KEY=
LITELLM_TIMEOUT_SECONDS=30
